// The hiring-manager interviews view reuses the recruiter implementation,
// which is backed by Firestore and lets panel members record their own
// scores and complete interviews.
export { InterviewsPage } from '../recruiter/InterviewsPage';
