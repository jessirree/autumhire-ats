// The hiring-manager candidate view is functionally identical to the
// recruiter one (documents, screening answers, status actions, panel
// comments, interview history), so we reuse that implementation instead of
// maintaining a second copy backed by mock data.
export { CandidateDetail } from '../recruiter/CandidateDetail';
